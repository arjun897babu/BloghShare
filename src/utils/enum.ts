export enum ResponseStatus {
  SUCCESS = "success",
  Error = "error",
}

export enum Cookie {
    access='token',
    refresh='rereshToken'
}

export enum Node_ENV{
    PROD='production',
    DEV='development'
}